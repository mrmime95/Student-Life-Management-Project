<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/tab_courses"
        android:enabled="true"
        android:icon="@drawable/ic_assignment_turned_in"
        app:showAsAction="ifRoom"
        android:title="Courses"/>
    <item
        android:id="@+id/tab_reminders"
        android:enabled="true"
        android:icon="@drawable/ic_today"
        app:showAsAction="ifRoom"
        android:title="Reminders"/>
    <item
        android:id="@+id/tab_timetable"
        android:enabled="true"
        android:title="Timetable"
        android:icon="@drawable/ic_account_box"
        app:showAsAction="ifRoom" />
</menu>
